name: TypeScript Action
author: Austen Stone
description: A TypeScript Action
branding:
  icon: "plus"
  color: "blue"

inputs:
  token:
    description: The GitHub token used to create an authenticated client
    default: ${{ github.token }}
    required: false

runs:
  using: "docker"
  image: "ghcr.io/austenstone/angular-codespace:v1.2"
  args:
    - ${{ inputs.token }}
